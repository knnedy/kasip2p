services:
  - type: web
    name: kasip2p-signal
    runtime: node
    rootDir: apps/signaling
    buildCommand: pnpm install && pnpm build
    startCommand: node dist/index.js
    envVars:
      - key: PORT
        value: 3001
