services:
  - type: web
    name: kasip2p-signal
    runtime: node
    rootDir: .
    buildCommand: pnpm install && pnpm --filter signaling build
    startCommand: node apps/signaling/dist/index.js
    envVars:
      - key: PORT
        value: 3001
